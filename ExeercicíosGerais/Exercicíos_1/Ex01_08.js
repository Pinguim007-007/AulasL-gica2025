const prompt = require('prompt-sync')();

v1 =parseFloat( prompt( 'insira o valorda alrgura'))
v1 =parseFloat( prompt( 'insira o valor do comprimento'))

console.log(`area: ${v1*v2}
    Tinta: ${(v1*v2/2)}`)